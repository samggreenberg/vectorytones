# Aggregated requirements for all results exporters.
#
# Each exporter lives in vtsearch/exporters/<name>/ and has its own
# requirements.txt listing any packages it needs beyond the core deps.
#
# To add a new exporter's dependencies, append a line here:
#   -r vtsearch/exporters/<name>/requirements.txt
#
# ── Built-in exporters ──────────────────────────────────────────────────────
-r vtsearch/exporters/gui/requirements.txt
-r vtsearch/exporters/file/requirements.txt
-r vtsearch/exporters/email_smtp/requirements.txt
